describe('bucket', function()
    local bucket = require'bucket'

    it('should have no return current position on a tock', function()
    
    end)

    it('should be able to move left when the controller is used', function()
    
    end)

    it('should be able to move right when the controller is used', function()
    
    end)

    it('should not be able to move left when the bucket is at the leftmost chute', function()
    
    end)

    it('should not be able to move right when the bucket is at the rightmost chute', function()
    
    end)

    it('should not be able to move more than once between tocks', function()
    
    end)

    it('should correctly report whether or not a ball is in the chute it is under to the controller', function()
    
    end)
end)